# Users will be able to search for tools by barcode, name (e.g. ”pipe wrench”), and
# category. By default, the resulting list of tools will be sorted alphabetical (ascending)
# by name.

from typing import List


# Given barcode
def inspect_tool(barcode: int) -> str:
    tool = ""
    return tool


# Given name
def inspect_tool(name: str) -> str:
    tool = ""
    return tool


# Given category
def inspect_tools(category: str) -> List[str]:
    tools = []
    return tools
